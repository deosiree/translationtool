# 列置空功能设计文档

## 需求概述

**功能名称**：列置空（清空空值）

**使用场景**：当外文翻译不合适时，需要将指定字段的值置空

**开发方式**：增量开发

## 功能架构

### 1. 组件结构

#### 1.1 父组件修改
- **文件**：`prototype/writeBackValidate/createVersionModal.vue`
- **修改位置**：`leftBottomBtn` 插槽（第64-77行）
- **新增内容**：添加"列置空"按钮

#### 1.2 新组件创建
- **文件**：`src/components/Button/clearFields.vue`
- **模式**：支持按钮模式和模态框模式
- **父组件调用方式**：按钮模式

### 2. 组件设计

#### 2.1 clearFields 组件 Props

```javascript
props: {
  // 数据源（已选词条列表）
  dataSource: {
    type: Array,
    required: true,
  },
  // 字段选项（与ExportButton保持一致）
  fieldOptions_: {
    type: Array,
    required: true,
  },
  // 组件模式：'button' | 'modal'
  mode: {
    type: String,
    default: 'button',
  },
  // 按钮模式下的按钮标题
  buttonTitle: {
    type: String,
    default: '列置空',
  },
  // 按钮尺寸
  size: {
    type: String,
    default: 'middle',
  },
  // 是否隐藏按钮（仅显示模态框）
  hideButton: {
    type: Boolean,
    default: false,
  },
}
```

#### 2.2 clearFields 组件 Events

```javascript
emits: [
  'update:dataSource',  // 更新数据源事件
  'afterClose',         // 模态框关闭后事件（可选）
]
```

#### 2.3 模态框UI设计

**参考来源**：`src/components/Button/exportButton.vue`

**保留内容**：
- "置空字段"选择器（对应原"导出字段"）
- 全选功能按钮

**删除内容**：
- 文件类型选择（exportType）
- 只能导出已审核复选框（statusCheck）
- XML相关字段（xml_name, local_desc）

**UI结构**：
```vue
<CustomModal 
  :modalTitle="'列置空'" 
  width="60%" 
  :visible="clearFieldsVisible" 
  :showCancel="false" 
  :showOk="false"
  @handleClose="handleClose">
  <div class="content">
    <a-form ref="clearFieldsForm" :model="clearFieldsModal">
      <a-form-item 
        label="置空字段" 
        name="field" 
        :rules="[{ required: true, message: '请选择!' }]">
        <div style="display: flex; justify-content: space-between;">
          <a-select 
            mode="multiple" 
            v-model:value="clearFieldsModal.field" 
            :options="fieldOptions"
            :fieldNames="{ label: 'label', value: 'label' }" 
            placeholder="请选择置空字段"
            allowClear 
            style="flex: 1; margin-right: 8px;" />
          <a-button 
            type="link" 
            size="small" 
            @click="selectAllFields" 
            style="font-size: smaller; margin-top: 0">
            全选
          </a-button>
        </div>
      </a-form-item>
    </a-form>
  </div>
  <template #leftBottomBtn>
    <a-button @click="handleClose">取消</a-button>
    <a-button type="primary" @click="handleOK" :loading="clearFieldsLoading">确定</a-button>
  </template>
</CustomModal>
```

### 3. 数据流设计

#### 3.1 字段过滤逻辑

**参考**：`exportButton.vue` 的 `mounted` 钩子（第122-142行）

**过滤规则**：
- 排除ID相关字段（"英文翻译id", "俄文翻译id", "西文翻译id", "法文翻译id", "中文翻译id"）
- 使用传入的 `fieldOptions_` prop，如果不存在则使用 `entryParams.exportFields` 作为后备

#### 3.2 API调用设计

**Mock API设计**：

**请求体结构**：
```javascript
{
  columnNames: string[],        // 需要置空的字段名数组（不包含"id"）
  entryInfoEntities: Array,     // 词条数据源
}
```

**注意**：
- 不需要 `exportType` 和 `excelName` 字段
- `columnNames` 不需要包含 "id"（与导出功能不同）

**响应体结构**：
```javascript
{
  data: {
    list: Array,  // 更新后的词条数据列表
  }
}
```

**API调用位置**：`handleOK` 方法中

**调用流程**：
1. 验证字段选择（必须至少选择一个字段）
2. 构建请求体（`columnNames` + `entryInfoEntities`）
3. 调用 mock API
4. 处理响应（获取 `res.data.list`）
5. 通过 `$emit('update:dataSource', updatedData)` 更新父组件
6. 显示成功通知（`notification.success`）
7. 关闭模态框

#### 3.3 父组件数据更新

**更新位置**：`createVersionModal.vue`

**更新方式**：
```vue
<ClearFields 
  :dataSource="dataSource" 
  :fieldOptions_="fieldOptions" 
  size="middle" 
  buttonTitle="列置空"
  @update:dataSource="handleDataSourceUpdate" />
```

**处理方法**：
```javascript
handleDataSourceUpdate(updatedData) {
  this.dataSource = updatedData;
  // 触发父组件的数据更新事件（如果需要）
  this.$emit('update:dataSource', updatedData);
}
```

### 4. 实现步骤

#### 步骤1：创建 clearFields 组件
- [ ] 创建 `src/components/Button/clearFields.vue`
- [ ] 实现按钮模式和模态框模式
- [ ] 复制并简化 `exportButton.vue` 的模态框UI
- [ ] 实现字段选择功能（保留全选功能）
- [ ] 实现字段过滤逻辑（排除ID字段）

#### 步骤2：实现API调用
- [ ] 创建 mock API 函数（参考 `entryExportByCondition` 的结构）
- [ ] 实现 `handleOK` 方法
- [ ] 实现数据验证逻辑
- [ ] 实现响应处理和事件触发
- [ ] 添加成功通知和错误处理

#### 步骤3：集成到父组件
- [ ] 在 `createVersionModal.vue` 的 `leftBottomBtn` 插槽中添加按钮
- [ ] 引入 `ClearFields` 组件
- [ ] 实现 `handleDataSourceUpdate` 方法
- [ ] 确保数据更新后表格自动刷新

#### 步骤4：测试验证
- [ ] 测试按钮点击打开模态框
- [ ] 测试字段选择功能
- [ ] 测试全选功能
- [ ] 测试API调用和数据更新
- [ ] 测试父组件表格数据刷新
- [ ] 测试错误处理（空字段选择、API失败等）

### 5. 技术细节

#### 5.1 字段过滤实现

```javascript
mounted() {
  this.$nextTick(() => {
    // 排除不能置空的列名（ID列）
    const filterColNames = [
      "英文翻译id",
      "俄文翻译id",
      "西文翻译id",
      "法文翻译id",
      "中文翻译id",
    ];
    // 使用传入的 fieldOptions_ prop，如果不存在则使用 entryParams.exportFields 作为后备
    const sourceOptions = this.fieldOptions_ && Array.isArray(this.fieldOptions_) && this.fieldOptions_.length > 0
      ? this.fieldOptions_
      : (entryParams && entryParams.exportFields ? entryParams.exportFields : []);
    this.fieldOptions = sourceOptions.filter(
      (item) => !filterColNames.includes(item.label)
    );
  });
}
```

#### 5.2 API调用实现

```javascript
async handleOK() {
  this.clearFieldsLoading = true;
  try {
    // 验证字段选择
    if (!this.clearFieldsModal.field || this.clearFieldsModal.field.length === 0) {
      message.error("请选择置空字段！");
      return;
    }
    
    // 构建请求体（注意：不需要包含"id"）
    const data = {
      columnNames: this.clearFieldsModal.field,
      entryInfoEntities: this.dataSource,
    };
    
    // 调用 mock API
    const res = await clearFieldsByCondition(data);
    
    // 处理响应
    if (res && res.data && res.data.list) {
      const updatedData = res.data.list;
      
      // 触发更新事件
      this.$emit('update:dataSource', updatedData);
      
      // 显示成功通知
      notification.success({
        message: '置空成功',
        description: `已成功将 ${this.clearFieldsModal.field.length} 个字段置空`,
      });
      
      // 关闭模态框
      this.handleClose();
    } else {
      message.error('置空失败：响应数据格式错误');
    }
  } catch (error) {
    console.error('置空失败：', error);
    message.error('置空失败：' + (error.message || '未知错误'));
  } finally {
    this.clearFieldsLoading = false;
  }
}
```

#### 5.3 Mock API 函数定义

**位置**：`prototype/writeBackValidate/http/api.js`

**函数签名**：
```javascript
export function clearFieldsByCondition(data) {
  // Mock实现：返回更新后的数据
  // 实际实现中，应该调用后端API
  return Promise.resolve({
    data: {
      list: data.entryInfoEntities.map(entry => {
        const updatedEntry = { ...entry };
        data.columnNames.forEach(columnName => {
          updatedEntry[columnName] = null; // 使用 null 置空
        });
        return updatedEntry;
      })
    }
  });
}
```

**注意**：响应格式为 `{ data: { list: Array } }`，与 `entryExportByCondition` 的二进制响应不同

### 6. 注意事项

1. **数据一致性**：确保置空后的数据与父组件表格显示保持一致
2. **字段映射**：注意字段的 `label` 和实际数据字段名的映射关系
3. **错误处理**：完善各种异常情况的处理（网络错误、数据格式错误等）
4. **用户体验**：添加加载状态、成功提示、错误提示等
5. **代码复用**：尽量复用 `exportButton.vue` 中的逻辑，减少重复代码

### 7. 已确认事项

1. **Mock API 响应格式**：✅ 不一致，mock api返回有效数据至 `res.data.list`，而 `entryExportByCondition` 返回的是二进制数据
2. **置空值类型**：✅ 使用 `null`
3. **API文件位置**：✅ Mock API 函数应放在 `prototype/writeBackValidate/http/api.js` 中
4. **字段映射**：确认 `fieldOptions` 中的 `label` 是否直接对应数据对象的属性名

## 设计验证

请确认以下内容是否符合预期：

1. ✅ 组件结构设计（按钮模式 + 模态框模式）
2. ✅ UI设计（简化版导出字段选择器）
3. ✅ 数据流设计（API调用 → 数据更新 → 表格刷新）
4. ✅ 实现步骤规划

如有需要调整的地方，请指出。
