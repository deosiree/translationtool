写一个原型设计：参考现有的“词条导入”模态框组件 `src/views/workbench/importModal.vue`，在**保持其它导入方式（文件/TS/实时库/辞典/枚举等）整体拷贝不变**的前提下，增强其中的“配置文件”导入方式：

- **改造位置**：`importModal.vue` 内 `dataType === 'config'` 的区块（原位置约 `212-230` 行附近），在 prototype 中对应 `prototype/importConfig/ImportModalPrototype.vue` 的同名区域。
- **新增 UI**：在“配置文件”导入区块中增加子 Tab：`XML` / `JSON`
  - Tab 一行，位于“配置文件”区域顶部。
  - Tab 下方一行显示整个“配置文件”表单行：左侧是“配置文件”多选下拉，右侧是“导入”按钮，整体为**水平布局的一行表单**（参考“实时库-对象数据/元数据/全量”的布局风格）。
- **子 Tab 内容**：
  - XML、JSON 两个 Tab 的表单结构**看起来相同**，都是：`配置文件（XML/JSON）：[多选下拉] + 全选/清空 + 导入按钮`，但**不复用同一套状态与数据源**：
    - XML Tab：使用真实接口 `getConfigList` 返回的配置文件列表（原有行为）。
    - JSON Tab：使用本地 Mock 的 `.json` 配置文件列表（例如：`base.json`、`workflow.json`、`glossary.json` 等），通过前端构造选项，不调用真实接口。
  - 允许 XML/JSON Tab **各自维护独立的已选项与搜索关键字**（切换 Tab 不丢选择）。
- **功能目标**：在拷贝版导入模态框中让“配置文件”导入支持 XML/JSON 两种配置文件入口，并可通过原型路由直接体验整个导入流程。