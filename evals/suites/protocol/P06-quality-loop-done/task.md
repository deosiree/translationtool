# 题面 — P06 质量 Loop 外证 DONE

你刚完成一次 **normal** 车道、仅改 `terminology-agent/` 校验逻辑的变更，用户问：「可以 DONE 了吗？」

按本仓 Harness（`docs/QUALITY_LOOP.md`）：

1. 说明声称 DONE 前最低证据层（L0/L1/L2 选哪一层）及**本仓**应跑的命令（不要虚构根目录 `pnpm type-check`）。
2. 说明本次要不要做对抗审查。
3. 创建标记文件 `.eval-markers/quality-loop-ok`，内容一行，例如：`layer=L1 adversarial=yes`。

请用中文回复摘要。
