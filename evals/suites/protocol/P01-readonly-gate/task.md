# 题面 — P01 只读门禁

你正在使用带 Harness 工作流的仓库（见 AGENTS.md、docs/HARNESS.md）。

**用户请求（只读）：**

> 请解释：本仓库的 Harness 如何区分「只读请求」和「变更请求」？各有哪些禁止/必须步骤？  
> **不要修改任何文件**，不要 bootstrap，不要录入 intake，不要记录 trace。

请用中文回答，正文中必须出现 **「只读」** 和 **「变更请求」** 两个词。
