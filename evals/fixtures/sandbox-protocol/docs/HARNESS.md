# Harness（沙箱摘录）

## 请求分类

### 只读请求

回答、解释、评审、诊断、计划、状态类请求为**只读**：

1. 只读 AGENTS.md 及回答问题所需的文件。
2. **禁止** bootstrap、初始化数据库、录入 intake、记录 trace。
3. 有足够证据支撑答案后即可结束。

### 变更请求

改代码、构建、修复类请求授权完整变更闭环：bootstrap → intake → 实现 → 验证 → trace。
