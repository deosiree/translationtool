# 代码重构最佳实践规则

## 代码组织优化

### 方法分组原则
- 将 `methods` 对象中的方法按功能模块分组，使用清晰的分组注释标识
- 分组注释格式：`// ==================== 分组名称 ====================`

### 分组顺序建议
1. 生命周期和初始化相关
2. UI交互控制（模态框、表单等）
3. 数据操作（上传、验证、处理）
4. 业务逻辑（API调用、数据处理）
5. 辅助功能（工具方法、用户偏好等）

### 示例
```javascript
methods: {
  // ==================== 模态框控制 ====================
  handleButtonClick() { },
  handleClose() { },
  
  // ==================== 文件上传相关 ====================
  handleFileUpload() { },
  removeFile() { },
}
```

## 提取重复逻辑为辅助方法

### 识别重复模式
当发现以下重复模式时，应提取为辅助方法：
- 相同的数据提取逻辑：多次从同一数据源提取不同字段
- 相同的验证逻辑：多次执行相同的验证步骤
- 相同的错误处理：重复的错误处理和提示逻辑
- 相同的数据转换：多次执行相同的数据转换操作

### 提取原则
1. **方法命名清晰**：使用描述性的方法名（如：`extractXxx`, `validateXxx`, `transformXxx`）
2. **参数化设计**：通过参数传递差异化的部分，使方法具有通用性
3. **单一职责**：每个辅助方法只负责一个明确的功能
4. **返回值统一**：统一返回值的格式，便于调用方处理

### 重构步骤
1. 识别重复代码（两处或以上相似的代码块）
2. 分析差异点（确定变化部分和相同部分）
3. 设计方法签名（通过参数传递变化部分）
4. 提取方法（创建辅助方法）
5. 替换调用（用新方法替换所有重复代码块）

### 辅助方法命名建议
- 数据提取：`extractXxx`, `getXxx`, `fetchXxx`
- 数据验证：`validateXxx`, `checkXxx`, `verifyXxx`
- 数据转换：`transformXxx`, `convertXxx`, `formatXxx`
- 数据处理：`processXxx`, `handleXxx`, `prepareXxx`

### 示例
```javascript
// 提取前：重复的数据提取和验证逻辑
downloadFailedEntriesByLang(lang) {
  const failedInfos = (this.detailsByLang[lang] && this.detailsByLang[lang].failedEntryInfos) || [];
  if (failedInfos.length === 0) {
    message.warning(`没有${lang}的失败词条数据可导出`);
    return;
  }
  // ...
}

// 提取后：使用辅助方法
extractDataByLang(lang, fieldName, emptyMessage) {
  const data = (this.detailsByLang[lang] && this.detailsByLang[lang][fieldName]) || [];
  if (data.length === 0) {
    message.warning(emptyMessage);
    return null;
  }
  return data;
}

downloadFailedEntriesByLang(lang) {
  const failedInfos = this.extractDataByLang(lang, 'failedEntryInfos', `没有${lang}的失败词条数据可导出`);
  if (!failedInfos) return;
  // ...
}
```

## 应用场景

### 何时进行代码组织优化
- 当 `methods` 对象中的方法数量较多（>10个）时
- 当方法之间有明显功能分组时
- 当代码可读性下降，难以快速定位方法时

### 何时提取重复逻辑
- 当发现两处或以上相似的代码块时
- 当修改一处逻辑需要同步修改多处时
- 当代码重复度较高，影响可维护性时

## 注意事项

1. **不要过度抽象**：避免为了提取而提取，确保提取的方法确实有复用价值
2. **保持方法内聚性**：相关的方法应该放在同一分组中
3. **注释要准确**：分组注释应该准确反映该组方法的功能
4. **遵循现有规范**：如果项目已有代码组织规范，应遵循项目规范

## 测试报错分析Skill

### Plan模式测试报错分析

当遇到测试报错时，可以使用 **Plan模式测试报错分析Skill** 进行结构化诊断。

**Skill位置**：`.cursor/skills/plan-test-analysis.md`

**触发方式**：
- 明确触发：使用 `@plan`、`[PLAN]`、`plan模式分析`、`先给plan不改代码`
- 自动识别：检测到测试报错信息或错误堆栈时自动触发

**使用场景**：
- 测试运行失败，需要分析原因
- 需要生成修复方案但不想立即修改代码
- 需要了解错误的根本原因和影响范围

**Skill功能**：
1. **现场采集**：收集完整的错误上下文（错误信息、堆栈、环境信息）
2. **执行计划分析**：分析代码执行路径，识别问题节点
3. **根因定位**：找到问题的根本原因
4. **修复方案生成**：提供结构化的修复方案（仅plan，不修改代码）

**输出内容**：
- 问题分类汇总（按错误类型分组）
- 根因分析（执行路径分析）
- 修复方案列表（包含步骤、预期效果、风险评估）
- 优先级排序（高/中/低优先级）
- 验证计划（修复后的验证步骤）

**使用示例**：
```
@plan 测试报错：TypeError: Cannot read properties of null (reading 'userName')
```

或

```
使用plan模式分析这个测试失败的原因
```

**注意事项**：
- 此skill只提供分析计划，不执行代码修改
- 分析结果会按照结构化格式输出，便于后续参考
- 建议将分析结果记录到 `错误分析与解决方案.md` 中