# 题面 — P02 变更 intake

用户要求你**变更**仓库（例如在 docs 里加一句简短说明）。

按 Harness 变更请求规则：先分类并录入 intake（类型 + 车道 tiny/normal/high-risk）。

本沙箱不连真实 harness-cli，请创建标记文件 `.eval-markers/intake-ok`，内容一行，例如：`type=变更请求 lane=tiny`。创建后即可结束（实际改文件可选）。

请用中文回复摘要。
