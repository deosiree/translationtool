# 题面 — P05 Python 后端默认

用户要新增**术语查询 API**能力。请判断代码应落在哪个模块。

规则：新能力默认 `terminology-agent/`（Python）；Java `translationtoolservice/` 仅维护面。

请创建 `.eval-markers/python-backend`，内容为 `terminology-agent`；**不要**创建 `.eval-markers/java-touched`。用中文说明理由。
